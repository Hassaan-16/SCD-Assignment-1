// ProjectPlanner.java
import java.io.*;
import java.time.*;
import java.util.*;
import java.util.stream.Collectors;

public class ProjectPlanner {

    Public static class Task{
        String name;
        LocalDate startDate;
        LocalDate endDate;
        String assignedTo;

        public Task(String name, LocalDate startDate, LocalDate endDate, String assignedTo) {
            this.name = name;
            this.startDate = startDate;
            this.endDate = endDate;

    }

    Public static class Resource{
        String name;
        String role;

        public Resource(String name, String role) {
            this.name = name;
            this.role = role;
        }
    }

    public static void main(String[] args) {
        List<Task> tasks = readTasksFromFile("tasks.txt");
        Map<String, List<Task>> tasksByAssignee = tasks.stream()
                .collect(Collectors.groupingBy(task -> task.assignedTo));

        for (String assignee : tasksByAssignee.keySet()) {
            System.out.println("

}